import { MemoryArena3D } from './MemoryArena3D';

/** Home playground — 3D graph arena + deposition + trap game + compare + desk. */
export function FieldPlayground() {
  return (
    <section className="field-playground">
      <MemoryArena3D />
    </section>
  );
}